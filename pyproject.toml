[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "emperor"
version = "0.1.0"
description = "A PyTorch-based deep learning framework"
readme = "README.md"
requires-python = ">=3.11,<3.14"
license = { text = "CC-BY-NC-4.0" }
authors = [{ name = "Emperor", email = "emperor@example.com" }]
keywords = ["machine learning", "deep learning", "pytorch", "transformers"]
dependencies = [
  "torch",
  "torchvision",
  "torchtext",
  "torchmetrics",
  "transformers",
  "datasets",
  "matplotlib",
  "ipython",
  "tensorboard",
  "setuptools",
  "pandas",
  "ipykernel",
  "numpy",
  "lightning",
  "torchinfo",
  "gymnasium",
  "fastapi>=0.139,<0.140",
  "starlette>=1.3,<1.4",
  "uvicorn[standard]>=0.51,<0.52",
  "pydantic-settings",
  "httpx>=0.28,<0.29",
]

[project.optional-dependencies]
dev = [
  "ruff>=0.8,<0.14",
]

[project.urls]
Homepage = "https://github.com/yourusername/emperor"
Repository = "https://github.com/yourusername/emperor"
Issues = "https://github.com/yourusername/emperor/issues"

[tool.setuptools.packages.find]
where = ["."]

[tool.ruff]
target-version = "py311"
line-length = 88
src = ["workbench/backend"]

[tool.ruff.lint]
select = ["E", "F", "I", "B", "UP"]
