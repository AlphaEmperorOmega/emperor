import { workbenchVisualizationTokens } from "@/lib/visual-tokens";

/** Palette cycled across overlaid run lines in multi-run charts. */
export const multiRunLineColors = workbenchVisualizationTokens.multiRunLineColors;

/** Palette cycled across overlaid scalar series in the logs charts. */
export const scalarSeriesColors = workbenchVisualizationTokens.scalarSeriesColors;
