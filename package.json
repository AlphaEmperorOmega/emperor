{
  "name": "emperor-development-tools",
  "private": true,
  "engines": {
    "node": ">=24 <25"
  },
  "scripts": {
    "typecheck:model-packages": "pyright --project pyright-model-packages.json",
    "typecheck:model-runtime": "pyright --project pyrightconfig.json src/model_runtime"
  },
  "devDependencies": {
    "pyright": "1.1.411"
  }
}
